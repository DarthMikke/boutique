{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "dev": "tsc && vite build --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.74.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
